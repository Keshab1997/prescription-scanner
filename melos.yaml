name: prescription_scanner_workspace
packages:
  - apps/*
  - packages/*

scripts:
  analyze:
    run: melos exec -- flutter analyze
  test:
    run: melos exec -- flutter test
  format:
    run: dart format .
